const filterUser = document.querySelector('#filter')

filterUser.addEventListener('input', filterUserTable)


function filterUserTable(){
  let userTable = document.querySelectorAll('.user')
  userTable.forEach(name=> {
    name =user
    console.log(name);
  })
}